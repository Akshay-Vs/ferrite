global:
  scrape_interval: 15s

# Accept remote-write from the collector
remote_write_receiver:
  enabled: true

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8888"]
